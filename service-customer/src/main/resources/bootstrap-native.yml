eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/

spring:
  cloud:
    config:
      discovery:
        enabled: true
        service-id: config-server
      uri: http://localhost:8504/
      fail-fast: true